# Encode a PNG file as Base64 for HTML embedding

Encode a PNG file as Base64 for HTML embedding

## Usage

``` r
encode_png_base64(file)
```

## Arguments

- file:

  Path to a PNG file.

## Value

A single Base64 string (no data:image/... prefix).
