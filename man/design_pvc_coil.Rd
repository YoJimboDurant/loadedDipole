% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loadedDipole.R
\name{design_pvc_coil}
\alias{design_pvc_coil}
\title{Design a single-layer air-core solenoid coil on PVC}
\usage{
design_pvc_coil(
  inductance,
  pvc_size = "1-1/2",
  wire_diameter = 0.064,
  turn_spacing = NULL,
  schedule = "40"
)
}
\arguments{
\item{inductance}{Target inductance in microhenry (uH).}

\item{pvc_size}{Nominal Schedule 40 PVC size ("1/2", "3/4", "1", "1-1/4", 
"1-1/2", "2").}

\item{wire_diameter}{Wire diameter in inches (e.g., 0.064 for AWG 14).}

\item{turn_spacing}{Optional spacing between turns in inches. If NULL,
assumes tight winding (spacing = wire diameter).}

\item{schedule}{Pipe schedule (only "40" supported).}
}
\value{
A list containing:
  \item{turns}{Estimated number of turns}
  \item{winding_length_in}{Total winding length in inches}
  \item{diameter_in}{Coil diameter in inches}
  \item{wire_length_ft}{Estimated wire length in feet}
  \item{summary_table}{Data frame of N ± 2 turns with inductance values}
}
\description{
This function computes the winding geometry, required number of turns,
winding length, and wire length for a target inductance using Wheeler's
formula. It also supports specifying wire diameter and optional turn
spacing, making it suitable for practical HF loading coil construction.
}
\details{
Wheeler (single-layer) formula:
  L (uH) = (d^2 * N^2) / (18 d + 40 l)
}
\examples{
design_pvc_coil(
  inductance = 33.5,
  pvc_size = "1-1/2",
  wire_diameter = 0.064,
  turn_spacing = 0.070
)
}
